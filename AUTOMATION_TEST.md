# MacroRelay 자동화 테스트 2.19.17-test

- 첨부 AHK에서는 특정 프로그램 설정을 가져오지 않고, 범용 `WM_MOUSEMOVE → WM_LBUTTONDOWN → WM_LBUTTONUP` 전송 순서만 참고했습니다.
- 자동 방식은 탐지 지점 아래의 실제 자식 창을 찾아 그 창의 클라이언트 좌표로 변환한 뒤 비활성 클릭합니다.
- `최상위 창 직접 메시지`는 자식 창 자동 선택 없이 지정한 최상위 창에 같은 메시지 순서를 전송하는 범용 고급 옵션입니다.

- 스마트 녹화 검토의 모든 클릭은 기본적으로 비활성(백그라운드) 클릭으로 생성되며 일괄 해제할 수 있습니다.
- 이미지 서치는 동일 크기를 네이티브 고속 경로로 먼저 확인하고 필요할 때만 OpenCV 보정 검색을 실행합니다.
- F8 캡처는 키를 누른 당시의 활성 창 대신 실제 선택 영역 아래의 창을 대상으로 사용합니다.
- 작은 아이콘은 최소 64×48 문맥 영역으로 보강하고, 기록 위치 주변을 먼저 검색한 뒤 창 전체를 확인합니다.
- 여러 창이 같은 실행 파일을 사용하면 창 제목·클래스까지 결합해 정확한 창을 선택합니다.
- 녹화 당시보다 창이 작아져도 상대 범위를 현재 창 안으로 보정하며 화면 좌상단으로 잘못 전환하지 않습니다.

## 스마트 녹화

- 검토표는 Ctrl/Shift로 여러 행을 선택한 뒤 `선택 행 제거` 또는 Delete 키로 한 번에 정리할 수 있습니다.
- 대기 행은 `ms` 숫자 입력기로 지연시간을 바로 수정하며, 내용 행 더블클릭 시 넓은 별도 편집창이 열립니다.
- 이미지 캡처 영역은 커서가 가까운 상·하·좌·우 테두리 또는 모서리를 휠로 개별 조절하며, 영역 중앙에서는 비율을 유지해 확대·축소합니다.
- `최근 녹화 검토 → 확인·영역 편집 → 이미지 상세 편집`에서 실제 저장 이미지를 크게 확인할 수 있습니다.
- 상세 편집은 컬러·흑백 구조·윤곽 특징 3개 분석 채널을 미리 보여주며, `자동 누끼`로 가운데 손·아이콘을 남기고 배경을 투명하게 만들 수 있습니다. 자동 결과가 애매하면 수동 편집에서 남길 대상을 먼저 드래그한 뒤 자동 누끼를 다시 실행할 수 있습니다.
- 자동 누끼의 동일한 투명 마스크가 원본 컬러·흑백 구조·윤곽 특징 미리보기에 모두 표시됩니다. 체크무늬 영역은 검색에서 제외되는 투명 배경입니다.
- 누끼가 적용되면 `수동 상세 편집` 버튼이 `누끼 결과 추가 편집`으로 바뀝니다. 자동 결과를 유지한 채 지우개·선택 투명화·색상 제거·흑백·이진화·밝기·대비를 계속 수정할 수 있습니다.
- 누끼로 투명해진 픽셀은 OpenCV 이미지 서치에서 비교 대상에서 제외되어, 배경이 바뀌어도 남긴 전경 모양을 기준으로 검색합니다.
- `클릭 위치 지정`을 켠 뒤 원본 미리보기의 손가락 끝처럼 실제 클릭할 곳을 누르면 중심 기준 오프셋이 자동 저장됩니다. 상세 설정에서 숫자 오프셋을 다시 계산할 필요가 없습니다.
- 정밀 검색은 이미지 크기의 70~150%를 빠르게 1차 탐색한 뒤 가장 유력한 크기 주변을 1% 단위로 다시 확인합니다. 전체 화면마다 모든 크기를 정밀 비교하지 않아 검색 지연을 줄였습니다.
- 감지된 이미지의 가로·세로 배율을 저장된 2점 오프셋에 자동 적용합니다. 이미지가 이동하거나 확대·축소돼도 손가락 끝처럼 지정한 상대 위치를 따라 클릭합니다.
- 상세 편집을 적용한 행은 한 번 캡처한 화면에서 컬러·흑백·윤곽 특징을 함께 비교하는 OpenCV 정밀 검색으로 자동 생성됩니다.

1. 매크로 빌더에서 노드를 추가할 매크로를 선택합니다.
2. `F9` 또는 `● 스마트 녹화`를 누릅니다.
3. 2초 뒤 준비되면 단독 `백틱`을 한 번 눌러 기록을 켭니다. 다시 누르면 즉시 일시정지되며, 키를 계속 누르고 있을 필요가 없습니다.
4. `Shift+백틱`은 `일반 액션`과 `분기 후보` 모드를 전환합니다. 상단 녹화 상태창에 현재 ON/OFF와 모드가 표시됩니다.
5. 일반 액션 모드에서 F8로 기본 이미지를 캡처한 뒤 분기 모드로 바꾸면, 클릭·키 입력·텍스트도 분기 액션으로 저장됩니다. F8을 여러 번 누르면 A 실패 → B → C 순서의 대체 이미지도 추가할 수 있습니다.
6. `F10`을 눌러 녹화 세션을 종료합니다.
7. 검토 화면에서 실제 이미지 썸네일을 확인하고 `확인·영역 편집`으로 범위를 조절합니다. 분기 후보는 별도 표시됩니다.
8. `노드로 추가`를 누르면 기본 이미지 성공은 공통 후속 동작으로, 실패는 다음 분기 후보로 자동 연결됩니다.

클릭 인식 방식은 다음 네 가지입니다.

- 창 활성화 후 클릭: 대상 창을 앞으로 가져온 뒤 기록한 클라이언트 좌표를 현재 창 위치로 환산하는 기본 방식입니다.
- 직접 캡처 이미지 찾아 클릭: 녹화 중 F8로 직접 칠해 지정한 영역을 그대로 저장하고, 실행 때 대상 창에서 찾아 이미지 중심을 클릭합니다.
- 이미지 찾아 클릭: 일반 클릭도 검토창의 썸네일 옆 `확인·영역 편집`을 눌러 어떤 이미지가 저장되는지 확인하고 범위를 조절할 수 있습니다.
- 백그라운드 클릭: ControlClick/PostMessage를 실제로 처리하는 프로그램에서만 사용합니다. Chromium 브라우저에는 권장하지 않습니다.
- 화면 절대 좌표: 창과 버튼 위치가 항상 고정된 환경에서만 사용합니다.

녹화 원본 JSONL 파일은 검토 화면을 열기 전에 메모리로 읽은 뒤 즉시 삭제합니다. 암호나 개인정보를 입력했다면 검토 화면에서 `입력한 텍스트를 노드에 저장`을 해제합니다.

검토창을 닫아도 매크로 빌더의 `최근 녹화 검토` 버튼으로 마지막 기록을 다시 열 수 있습니다.

## 자동 설정

액션을 선택하고 `⚡ 자동 설정`을 누르면 좌표 선택, 창 선택, 이미지 캡처, 텍스트 입력, 대기 시간 또는 프로그램 경로를 안내에 따라 구성합니다.

## 비활성 클릭 핸들 실험실

- 핸들 실험실은 단계 설정이나 검토창 내부가 아니라, 매크로 빌더 상단 `최근 녹화 검토` 버튼 바로 오른쪽에 독립 버튼으로 있습니다.
- `비활성 클릭 핸들 실험실`을 누르고 화면에서 대상 위치를 직접 지정하면 핸들 후보를 수집합니다.
- Studio가 Window Spy 방식으로 최상위 HWND, 커서 아래 자식 HWND, 겹친 자식 HWND와 ClassNN을 수집합니다.
- 핸들 목록이 열린 뒤 `시험 클릭 좌표 지정`을 눌러 같은 대상 창 안의 정확한 클릭 위치를 별도로 지정합니다.
- 실험실에는 시험 위치의 화면 좌표와 대상 창 기준 클라이언트 좌표가 함께 표시됩니다. 좌표를 지정하기 전에는 시험 클릭 버튼이 비활성화됩니다.
- 후보별 `시험 클릭`은 지정한 좌표로 창을 활성화하지 않고 `WM_MOUSEMOVE → WM_LBUTTONDOWN → WM_LBUTTONUP`을 전송합니다.
- 실제 반응이 확인된 후보를 `이 핸들·좌표 사용`으로 저장하면 프로그램별 핸들 프로필로 보관됩니다.
- 이후 같은 프로그램을 스마트 녹화하면 저장한 ClassNN/HWND를 자동으로 불러오고, 각 녹화 클릭의 좌표를 결합해 실행 데이터에 포함합니다.
- 이미지 인식 행은 이미지 찾기 방식은 그대로 유지하고, 이미지를 찾은 뒤 클릭하는 부분에만 저장된 핸들 엔진을 사용합니다.
- 관리자 권한 수준이 다른 창이나 자체적으로 외부 메시지를 차단하는 프로그램은 시험 클릭을 무시할 수 있습니다.

## 단계 테스트와 진단

- 빌더 상단의 `■ 정지`는 Studio에서 실행한 현재 매크로와 이미지 검색 하위 프로세스를 즉시 함께 종료합니다. 실행 중에만 활성화되며, 중복 실행을 막기 위해 실행 버튼은 잠시 비활성화됩니다.
- `▶ 선택 단계 테스트`: 선택한 노드만 별도 스크립트로 실행합니다. 좌표와 화면 검색 범위가 있는 경우 실행 전에 오버레이로 표시합니다.
- `▶ 이 단계만 테스트` 오른쪽의 `⌖` 아이콘에 커서를 올리면 배율·오프셋까지 적용된 최근 최종 클릭 좌표 주변과 십자선이 자동 표시됩니다. 아이콘을 클릭하면 팝업이 고정되며 다시 누르면 닫힙니다. 클릭 기록이 없으면 아이콘이 비활성화됩니다.
- 전체 실행과 단계 테스트 중 현재 실행 중인 노드는 굵은 청록색 테두리와 `실행 중` 표시로 구분됩니다.
- 이미지 서치 노드의 `▧` 아이콘에 커서를 올리면 원본 이미지가 별도 미리보기 팝업으로 표시됩니다.
- `✓ 자동 진단`: 잘못된 연결, 누락 이미지, 대상 창, 검색 제한 시간, 도달 불가능 노드와 최근 실행 실패를 검사합니다.
- 자동 수정 가능한 항목은 선택 후 한 번에 적용할 수 있습니다.

## 자동화 블록

노드를 여러 개 선택하고 `블록 저장`을 누르면 재사용 블록으로 저장됩니다. 다른 매크로에서 `＋ 블록 추가`를 눌러 내부 연결을 유지한 채 삽입할 수 있습니다.

## 대기 노드 일괄 변경

- 노드 플로우에서 Shift/Ctrl 또는 드래그로 대기 노드를 여러 개 선택한 뒤 선택된 대기 노드를 우클릭합니다.
- `선택한 대기 노드 시간 변경`은 선택 항목 중 대기 노드만 같은 시간으로 변경합니다.
- `모든 대기 노드 시간 변경`은 현재 매크로에 있는 모든 대기 노드를 같은 시간으로 변경합니다.

## 테스트 시 주의

- 관리자 권한으로 실행 중인 대상 프로그램은 동일한 권한의 Studio에서만 입력 기록이 가능할 수 있습니다.
- 이미지 방식은 클릭 직전 화면을 캡처합니다. 애니메이션·동영상처럼 모양이 계속 변하는 부분보다는 고정된 아이콘이나 버튼을 선택하세요.
- 스마트 녹화는 단독 `백틱`으로 기록 ON/OFF를 전환합니다. `Shift+백틱`은 일반 액션과 분기 후보 모드를 바꾸며 두 제어키는 대상 앱에 전달하거나 매크로 키 동작으로 저장하지 않습니다.
- 분기 후보 모드에서도 클릭·키 입력·텍스트를 기록하며 검토창에 `분기 클릭`, `분기 텍스트`처럼 구분해 표시합니다. 이미지 영역을 편집하는 동안 생긴 시간은 불필요한 대기 노드로 만들지 않습니다.
- 이미지 서치 노드를 여러 개 선택하고 노드 플로우 상단의 `시작 검색 묶기`를 누르면 번호순으로 검색합니다. 미탐지일 때만 다음 후보로 넘어가며, 탐지 성공 시 해당 노드의 성공 흐름만 실행합니다.
- 노드를 선택한 상태에서 `노드 추가`를 누르면 마지막 번호가 아니라 선택 노드의 성공 흐름에 자동 삽입됩니다. 기존 성공 연결이 있으면 `선택 노드 → 새 노드 → 기존 대상` 순서로 보존됩니다.
- `F10`은 기록 ON/OFF와 관계없이 언제든 녹화 세션을 종료합니다.
- 스마트 녹화 상태창은 화면 끝이 아니라 MacroRelay 메인 창의 오른쪽에 상세 설정창과 같은 기준으로 붙습니다. 오른쪽 공간이 부족하면 왼쪽 또는 현재 화면 안쪽으로 자동 보정됩니다.
